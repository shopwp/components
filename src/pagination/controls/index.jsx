import PaginationLoadMore from "./load-more"

function PaginationControls() {
  return (
    <section className="swp-pagination-controls wps-pagination-controls">
      <PaginationLoadMore />
    </section>
  )
}

export default PaginationControls
